# Twist2 Neck Demo ROS2 package
Will clean up readme later

For now, important stuff
- To run camera_viz, you need to have a conda environment named
  camera_env created and set up with camera_viz's setup script
  command: `./camera_viz.sh setup --venv "$CONDA_PREFIX" --jetson --no-oakd`
- You need to have the isaacteleop 1.5 installed on your machine
- This package was tested with a baremetal ros installation

To do: write docs and stuff